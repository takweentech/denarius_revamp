export interface AppConfig {
    name: string;
    logoPath: string;
    theme: {
        primaryColor: string;
        secondaryColor: string;
        dangerColor: string;
        successColor: string;
    }
}