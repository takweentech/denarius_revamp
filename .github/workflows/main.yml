name: Test Self-Hosted Runner

on:
  push:
    branches:
      - development

jobs:
  test-runner:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "✅ Runner is working on $(hostname) at $(date)"

      - name: List files in repo
        run: ls -la

      - name: Create a test file
        run: echo "This is a runner test" > runner-test.txt
